#figure(
  image("../figs/images/summary_resolved_p99_overall_max_100y.svg", width: 100%),
  caption: [
      99th percentile wait of resolved patients and overall maximum wait under each algorithm in the main simulation, over 100 year simulation. The maximum is taken over all patients, including those still waiting when the simulation ended.
  ],
) <fig-max-wait-100y>
