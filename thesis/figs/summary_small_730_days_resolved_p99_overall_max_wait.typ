#figure(
  image("../figs/images/summary_small_730_days_resolved_p99_overall_max_wait.png", width: 100%),
  caption: [
      99th percentile wait of resolved patients and overall maximum wait under each algorithm in the small simulation with exponential variants, over 730 days. The maximum is taken over all patients, including those still waiting when the simulation ended.
  ],
) <fig-max-wait-10yr>
