#figure(
  image("../figs/images/summary_resolved_p99_overall_max_2y.svg", width: 100%),
  caption: [
      99th percentile wait of resolved patients and overall maximum wait under each algorithm in the small simulation with exponential variants, over two year simulation. The maximum is taken over all patients, including those still waiting when the simulation ended.
  ],
) <fig-max-wait-2y>
